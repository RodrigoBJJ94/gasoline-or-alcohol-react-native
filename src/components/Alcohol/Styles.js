import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    block: {
        marginBottom: 10,
    },
    textInput: {
        borderColor: 'black',
        borderWidth: 2,
        borderRadius: 4,
        height: 40,
    },
    text: {
        marginBottom: 10,
    }
});
